# SETUP

- 1. RUN tables_allowed from Database_debug to generate config file

# NB:

- 1. better to run composer directly in the container
- 2. run compose dump-autoload after adding new namespace into psr-4 in composer.json
